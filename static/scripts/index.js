window.onload = function() {
	// var tabs = document.getElementsByClassName('tabs')[0];
	// if(tabs) {
	// 	children = tabs.children;
	// 	for(i=0; i < children.length; i++) {
	// 		children[i].onclick = function() {
	// 			current = this;
	// 			for(k=0; k < children.length; k++) {
					
	// 				if(children[k].childNodes != null)
	// 				{
	// 					tabbed_content = children[k].children;
	// 					for(j=0; j < tabbed_content.length; j++) {
	// 						if(children[k] !== current) {
	// 							tabbed_content[j].style.display = 'none';
	// 						}
	// 						else
	// 							{ tabbed_content[j].style.display = 'block';}
	// 					}
	// 				}
					
	// 			}
	// 		}
	// 	}
	// }

	var alert = document.getElementsByClassName('alert')[0];
	console.log('alert ' + alert);


}